# Data files

## `import/`

The spreadsheets (.xlsx) used for the one-time import live here. This folder is
git-ignored because it contains customer data. The import itself is done; the
scripts in `scripts/import/` are kept so it could be repeated.

The instructor list is built from the names in the imported sheets (see
"Decisions that differ from the brief" in the main README).
